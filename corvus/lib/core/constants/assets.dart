abstract class AppAssets {
  AppAssets._();

  static const String _iconPath = 'assets/icons';

  // Placeholder icons (SVGs we'll use Lucide Icons for instead)
  static const String appLogo = '$_iconPath/corvus_logo.svg';
  static const String appIcon = '$_iconPath/corvus_icon.svg';
}
